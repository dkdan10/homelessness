const express = require("express")
const router = express.Router()

// const handleCreateMessage = require("./messages/createMessage")
// const handleGetConversationMessages = require("./messages/getMessages")

// const passport = require('passport');

// router.post('/create', passport.authenticate('jwt', { session: false }), handleCreateMessage)

// router.get('/getMessages', passport.authenticate('jwt', { session: false }), handleGetConversationMessages)

module.exports = router;
